<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewmodels="clr-namespace:App_da_Foto.ViewModels" 
             x:DataType="viewmodels:FotografoPerfilViewModel"
             x:Class="App_da_Foto.Views.FotografoPerfilPage"
             Title="{Binding Nome}"
             Visual="Material">

    <StackLayout Spacing="40" Padding="15,30,15,15">
        <StackLayout>
            <Label HorizontalOptions="Center" Text="Nome do Fotógrafo" FontSize="Large"/>
            <Label HorizontalOptions="Center" Text="{Binding Nome}" FontSize="35" FontAttributes="Bold"/>
        </StackLayout>

        <StackLayout>
            <Label HorizontalOptions="Center" Text="Especialidade" FontSize="Large" />
            <Label HorizontalOptions="Center" Text="{Binding Especialidade}" FontSize="35" FontAttributes="Bold"/>
        </StackLayout>
    </StackLayout>
    
</ContentPage>